# almari/social/urls.py

from django.urls import path
from . import views

app_name = 'social'

urlpatterns = [
    # Define your URLs here, for example:
    # path('login/', views.login_view, name='login'),
]
